import { FormData } from '@/types/formTypes';
import { createClient } from '@/utils/supabase/client';

export default async function editQuestion(formData:FormData, questionId:number){
console.log(formData)
}